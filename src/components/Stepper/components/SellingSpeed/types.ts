import { Step } from "@/enums";
import { IStepConfigComponentProps } from "../../types";

export interface IOption {
  text: string;
  icon: string;
}
export type ISellingSpeedProps = IStepConfigComponentProps;
